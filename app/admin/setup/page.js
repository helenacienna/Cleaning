import SetupChecklistClient from './SetupChecklistClient';

export const metadata = {
  title: 'Setup checklist · Cienna Cleaning',
};

export default function SetupChecklistPage() {
  return <SetupChecklistClient />;
}
